services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: securesight_api
    ports:
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - recognition-data:/app/data
      - avatars-data:/app/avatars
      - snapshots-data:/app/snapshots
    environment:
      - DB_PATH=/app/data/recognition.db
      - AVATARS_DIR=/app/avatars
      - SNAPSHOTS_DIR=/app/snapshots
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:3000,http://127.0.0.1:3000,https://localhost:3443}
      - JWT_SECRET=${JWT_SECRET:-}
      - CAMERA_API_KEY=${CAMERA_API_KEY:-}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-securesight2026}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5001/health')"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: securesight_web
    ports:
      - "3000:3000"
      - "3443:3443"
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy

  mediamtx:
    image: bluenviron/mediamtx:latest
    container_name: securesight_mediamtx
    ports:
      - "8889:8889"
      - "8189:8189/udp"
      - "9997:9997"
    restart: unless-stopped
    volumes:
      - ./mediamtx.yml:/mediamtx.yml:ro

volumes:
  recognition-data:
  avatars-data:
  snapshots-data:
