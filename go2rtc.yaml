api:
  listen: ":1984"

webrtc:
  listen: ":8555/tcp"
  ice_servers:
    - urls: [stun:stun.l.google.com:19302]

# Streams are created dynamically via WHIP from browser cameras.
# No static stream config needed.
streams: {}
